# Copyright Advanced Micro Devices, Inc.
# SPDX-License-Identifier: MIT
#
# Example private Helm values for glm-5-3. Copy this file, replace api_base,
# then deploy with LITELLM_VALUES_FILE. Skip interactive /models discovery.
#
# api_key: dummy is a placeholder required by openai/* routes.

modelList:
  - model_name: glm-5-3
    litellm_params:
      model: openai/glm-5-3
      api_base: https://example.invalid/v1
      api_key: dummy
    model_info:
      supports_native_streaming: true
